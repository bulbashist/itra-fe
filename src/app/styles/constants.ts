export enum CSSPadding {
  Tiny = 1,
  Small = 2,
  Average = 3,
  Decent = 5,
  Large = 8,
}

export enum CSSGap {
  Tiny = 1,
  Small = 2,
  Average = 3,
  Decent = 4,
  Large = 5,
}

export enum CSSMargin {
  Tiny = 1,
  Small = 2,
  Average = 3,
  Decent = 5,
  Large = 8,
}

export enum FontWeight {
  Bold = 600,
}

export enum FontSize {
  Small = 12,
}
